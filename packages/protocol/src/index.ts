/**
 * @crow/protocol — shared wire types for Crow.
 *
 * JSON-RPC 2.0 over newline-delimited WebSocket frames between the desktop
 * hub and per-host daemons, plus A2A (daemon-to-daemon) types.
 *
 * Filled in during P1; this stub keeps the workspace compiling.
 */
export const PROTOCOL_VERSION = "0.1.0" as const;

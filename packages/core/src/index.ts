/**
 * @crow/core — Crow agent runtime built on the pi SDK
 * (@mariozechner/pi-ai + @mariozechner/pi-agent-core).
 *
 * Owns: session factory, default tool set, skill loader, MCP config,
 * sub-agent runner, team presets. Filled in during P1/P4.
 */
export const CORE_VERSION = "0.1.0" as const;
